# ===========================================
# MiroFish Backend Dependencies
# ===========================================
# Python 3.11.x required (NOT 3.12+ — camel-oasis has no wheel for 3.12)
# Install: pip install -r requirements.txt
# ===========================================

# --- Core web framework ---
flask>=3.0.0
flask-cors>=6.0.0

# --- LLM / HTTP ---
# OpenAI-compatible SDK (used for Ollama and other local endpoints)
openai>=1.0.0
# HTTP client for embedding APIs
requests>=2.28.0

# --- Neo4j graph database ---
neo4j>=5.15.0

# --- Graphiti (optional memory engine; enable with GRAPH_BACKEND=graphiti) ---
graphiti-core>=0.28.0,<0.29

# --- OASIS social simulation (CAMEL) ---
camel-oasis==0.2.5
camel-ai==0.2.78

# --- File parsing ---
PyMuPDF>=1.24.0
# Encoding detection for non-UTF-8 text files
charset-normalizer>=3.0.0
chardet>=5.0.0

# --- Utilities ---
python-dotenv>=1.0.0
pydantic>=2.0.0
